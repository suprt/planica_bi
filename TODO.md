# TODO: Реализация недостающего функционала по ТЗ

**Дата создания:** 2025-11-15  
**Основано на:** `TZ_COMPLIANCE_REPORT.md`

---

## 🔴 Критичные задачи (для соответствия ТЗ)

### 1. Реализация Cron Scheduler для синхронизации
**Приоритет:** Критичный  
**Статус:** ❌ Не реализовано (только заглушка)  
**ТЗ:** Пункт 5, актуализация - ежедневная синхронизация и месячная фиксация

**Задачи:**
- [ ] Установить библиотеку `github.com/robfig/cron/v3`
- [ ] Реализовать `StartDailySync()` в `backend/internal/cron/scheduler.go`
  - Расписание: ежедневно в 02:00 MSK
  - Вызов `SyncAllProjects` для обновления данных текущего месяца (M)
- [ ] Реализовать `StartMonthlyFinalization()` в `backend/internal/cron/scheduler.go`
  - Расписание: 1-го числа каждого месяца в 07:00 MSK
  - Фиксация итогов прошедшего месяца, обновление окна M/M-1/M-2
- [ ] Интегрировать scheduler в `main.go` (запуск при старте приложения)
- [ ] Добавить обработку ошибок и логирование выполнения задач
- [ ] Протестировать выполнение задач

**Файлы для изменения:**
- `backend/internal/cron/scheduler.go`
- `backend/cmd/api/main.go`
- `go.mod` (добавить зависимость)

---

### 2. Публичная ссылка для клиентов
**Приоритет:** Критичный  
**Статус:** ❌ Не реализовано  
**ТЗ:** Актуализация - "клиент видит публичную ссылку отчёта (только просмотр)"

**Задачи:**
- [ ] Добавить поле `public_token` в модель `Project` (уникальный токен для публичного доступа)
- [ ] Создать миграцию для добавления поля `public_token` в таблицу `projects`
- [ ] Реализовать генерацию токена при создании/обновлении проекта
- [ ] Создать публичный эндпоинт `GET /api/public/report/{token}`
- [ ] Убрать требование авторизации для публичного эндпоинта
- [ ] Добавить валидацию токена в handler
- [ ] Обновить `ProjectService` для генерации токена
- [ ] Добавить возможность включения/отключения публичного доступа через `is_public` флаг (опционально)
- [ ] Протестировать публичный доступ

**Файлы для изменения:**
- `backend/internal/models/project.go`
- `backend/internal/repositories/project_repository.go`
- `backend/internal/services/project_service.go`
- `backend/internal/handlers/report_handler.go`
- `backend/internal/router/router.go`
- `backend/database/init/02_migrations.sql` (или новая миграция)

---

### 3. Очереди для API-вызовов (Asynq + Redis)
**Приоритет:** Критичный  
**Статус:** ❌ Полностью отсутствуют  
**ТЗ:** Пункт 5 - "Очереди: Laravel Queue (database/redis) для API‑вызовов и парсинга отчётов"

**Задачи:**
- [ ] Установить библиотеку `github.com/hibiken/asynq`
- [ ] Установить Redis клиент `github.com/redis/go-redis/v9`
- [ ] Добавить Redis в `docker-compose.yml`
- [ ] Создать `backend/internal/queue/client.go` для инициализации asynq клиента
- [ ] Создать `backend/internal/queue/worker.go` для обработки задач
- [ ] Создать типы задач:
  - [ ] `SyncMetricaDataTask` - синхронизация данных Метрики
  - [ ] `SyncDirectDataTask` - синхронизация данных Директа
  - [ ] `ParseReportTask` - парсинг отчётов
- [ ] Рефакторинг `SyncService`:
  - [ ] Вынести синхронизацию Метрики в очередь
  - [ ] Вынести синхронизацию Директа в очередь
  - [ ] Добавить retry механизм для неудачных API-вызовов
- [ ] Обновить `SyncHandler` для постановки задач в очередь вместо синхронного выполнения
- [ ] Настроить воркеры в `main.go` (запуск отдельного процесса или горутины)
- [ ] Добавить мониторинг очередей (опционально - asynqmon)
- [ ] Протестировать работу очередей

**Файлы для изменения:**
- `backend/internal/queue/` (новая директория)
- `backend/internal/services/sync_service.go`
- `backend/internal/handlers/sync_handler.go`
- `backend/cmd/api/main.go`
- `backend/cmd/worker/main.go` (новый файл для воркера)
- `docker-compose.yml` (добавить Redis)
- `go.mod` (добавить зависимости)

---

### 4. Админ-панель (Filament или альтернатива)
**Приоритет:** Критичный (но можно заменить на простой интерфейс)  
**Статус:** ❌ Не реализовано  
**ТЗ:** Актуализация - "админ управляет проектами через Filament"

**Варианты реализации:**
- **Вариант 1:** Простой HTML+JS админ-интерфейс (рекомендуется для MVP)
- **Вариант 2:** Использовать готовый админ-интерфейс (AdminJS, если есть для Go)
- **Вариант 3:** Интегрировать Filament (если есть для Go)

**Задачи (для варианта 1 - простой интерфейс):**
- [ ] Создать `frontend/admin/` директорию
- [ ] Создать HTML страницы:
  - [ ] `index.html` - список проектов
  - [ ] `project.html` - создание/редактирование проекта
  - [ ] `users.html` - управление пользователями и ролями
- [ ] Реализовать JS для работы с API
- [ ] Добавить базовую авторизацию в админ-панели
- [ ] Настроить роутинг для статических файлов в Echo
- [ ] Протестировать админ-панель

**Файлы для создания:**
- `frontend/admin/` (новая директория)
- `backend/internal/router/router.go` (добавить статические файлы)

---

## 🟡 Важные задачи (для производительности)

### 5. Логирование в файлы
**Приоритет:** Важный  
**Статус:** ⚠️ Частично реализовано (только stdout)  
**ТЗ:** Пункт 5 - "Логирование: storage/logs + уведомления об ошибках в Telegram/Sentry (опция)"

**Задачи:**
- [ ] Настроить zap для записи в файл `storage/logs/app.log`
- [ ] Добавить ротацию логов (lumberjack или встроенная ротация)
- [ ] Настроить разные уровни логирования для файла и stdout
- [ ] Создать директорию `storage/logs` при старте приложения
- [ ] Добавить конфигурацию для путей к логам в `config.go`
- [ ] Обновить `.dockerignore` и `.gitignore` для исключения логов
- [ ] Протестировать запись логов в файл

**Опционально:**
- [ ] Интеграция с Telegram Bot API для критических ошибок
- [ ] Интеграция с Sentry для мониторинга ошибок

**Файлы для изменения:**
- `backend/internal/logger/logger.go`
- `backend/internal/config/config.go`
- `backend/cmd/api/main.go`

---

### 6. Кэш Redis для справочников
**Приоритет:** Важный  
**Статус:** ❌ Полностью отсутствует  
**ТЗ:** Пункт 5 - "Кэш: Redis для кэширования неизменных справочников (кампании и пр.)"

**Задачи:**
- [ ] Подключить Redis клиент (уже будет установлен для очередей)
- [ ] Создать `backend/internal/cache/cache.go` для работы с Redis
- [ ] Реализовать методы:
  - [ ] `Get(key)` - получение из кэша
  - [ ] `Set(key, value, ttl)` - сохранение в кэш
  - [ ] `Delete(key)` - удаление из кэша
  - [ ] `InvalidatePattern(pattern)` - инвалидация по паттерну
- [ ] Добавить кэширование в репозитории:
  - [ ] `CounterRepository.GetCountersByProjectID` - TTL: 1 час
  - [ ] `GoalRepository.GetGoalsByCounterID` - TTL: 1 час
  - [ ] `DirectRepository.GetAccountsByProjectID` - TTL: 1 час
  - [ ] `DirectRepository.GetCampaignsByAccountID` - TTL: 1 час
- [ ] Инвалидировать кэш при обновлении данных (Create/Update/Delete)
- [ ] Добавить конфигурацию TTL в `config.go`
- [ ] Протестировать работу кэша

**Файлы для изменения:**
- `backend/internal/cache/` (новая директория)
- `backend/internal/repositories/counter_repository.go`
- `backend/internal/repositories/goal_repository.go`
- `backend/internal/repositories/direct_repository.go`
- `backend/internal/config/config.go`

---

## 🟢 Желательные задачи (для полноты MVP)

### 7. Базовый Frontend для просмотра отчётов
**Приоритет:** Желательный  
**Статус:** ❌ Не реализовано  
**ТЗ:** Пункт 8 - "Frontend (MVP): HTML + нативный JS"

**Задачи:**
- [ ] Создать `frontend/public/` директорию
- [ ] Создать HTML страницы:
  - [ ] `index.html` - главная страница с авторизацией
  - [ ] `report.html` - просмотр отчёта
  - [ ] `public-report.html` - публичный просмотр отчёта (по токену)
- [ ] Реализовать JS для:
  - [ ] Авторизации (login/register)
  - [ ] Получения списка проектов
  - [ ] Отображения отчёта (таблицы, графики)
  - [ ] Работы с публичными ссылками
- [ ] Добавить базовые стили (CSS)
- [ ] Настроить роутинг для статических файлов в Echo
- [ ] Протестировать frontend

**Файлы для создания:**
- `frontend/public/` (новая директория)
- `backend/internal/router/router.go` (добавить статические файлы)

---

### 8. Уведомления в Telegram/Sentry (опционально)
**Приоритет:** Низкий (опционально)  
**Статус:** ❌ Не реализовано  
**ТЗ:** Пункт 5 - "уведомления об ошибках в Telegram/Sentry (опция)"

**Задачи:**
- [ ] Интеграция с Telegram Bot API:
  - [ ] Добавить конфигурацию для Telegram Bot Token и Chat ID
  - [ ] Создать `backend/internal/notifications/telegram.go`
  - [ ] Отправка уведомлений при критических ошибках
- [ ] Интеграция с Sentry:
  - [ ] Установить `github.com/getsentry/sentry-go`
  - [ ] Настроить инициализацию Sentry в `main.go`
  - [ ] Добавить конфигурацию для Sentry DSN

**Файлы для изменения:**
- `backend/internal/notifications/` (новая директория)
- `backend/internal/config/config.go`
- `backend/cmd/api/main.go`

---

## 📊 Статистика выполнения

**Всего задач:** 8 основных блоков  
**Критичных:** 4  
**Важных:** 2  
**Желательных:** 2

**Текущий прогресс:** ~60% соответствия ТЗ

---

## 🔄 Порядок выполнения (рекомендуемый)

1. **Очереди (Asynq + Redis)** - основа для асинхронной обработки
2. **Cron Scheduler** - использует очереди для синхронизации
3. **Публичная ссылка** - быстро реализуется, критично для клиентов
4. **Админ-панель** - простой HTML интерфейс
5. **Логирование в файлы** - важно для продакшена
6. **Кэш Redis** - улучшает производительность
7. **Frontend** - для полноты MVP
8. **Уведомления** - опционально

---

## 📝 Примечания

- Все задачи должны быть протестированы после реализации
- При добавлении новых зависимостей обновлять `go.mod` и `go.sum`
- При изменении структуры БД создавать миграции
- Обновлять документацию при добавлении нового функционала

