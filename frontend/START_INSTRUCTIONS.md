# Инструкция по запуску приложения

## Проблема решена ✅

Исправлена версия `react-scripts` в `package.json` (было `^0.0.0`, стало `5.0.1`).

## Способы запуска

### Вариант 1: Через WebStorm (рекомендуется)
1. Откройте проект в WebStorm
2. В терминале WebStorm выполните:
   ```bash
   npm install
   npm start
   ```

### Вариант 2: Через командную строку
1. Откройте PowerShell или CMD в папке проекта
2. Убедитесь, что Node.js установлен:
   ```bash
   node --version
   npm --version
   ```
3. Если Node.js не установлен, скачайте с [nodejs.org](https://nodejs.org/)
4. Установите зависимости:
   ```bash
   npm install
   ```
5. Запустите приложение:
   ```bash
   npm start
   ```

### Вариант 3: Если npm не в PATH
Если команды `npm` и `node` не распознаются:
1. Установите Node.js с официального сайта
2. Перезапустите терминал/WebStorm
3. Или используйте полный путь к npm (обычно `C:\Program Files\nodejs\npm.cmd`)

## После запуска

Приложение откроется автоматически в браузере по адресу:
- **http://localhost:3000**

Если браузер не открылся автоматически, откройте этот адрес вручную.

## Возможные проблемы

### Порт 3000 занят
Если порт занят, React предложит использовать другой порт (например, 3001).

### Ошибки при установке зависимостей
Попробуйте:
```bash
npm cache clean --force
npm install
```

### Проблемы с TypeScript
Убедитесь, что все файлы сохранены и нет синтаксических ошибок.

## Проверка работоспособности

После запуска вы должны увидеть:
- ✅ Интерфейс Planica с тёмной темой
- ✅ Работающее меню пользователя
- ✅ Переключатель темы (🌙/☀️)
- ✅ Уведомления
- ✅ Боковое меню навигации

