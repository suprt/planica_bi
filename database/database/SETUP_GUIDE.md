# 📋 Полное руководство по настройке базы данных Planica BI

## 🔍 Обнаруженные проблемы и их решения

### ❌ Критические проблемы (требуют исправления)

#### 1. **Проблема в down миграции 001_create_database.down.sql**
**Проблема:** Попытка вставить запись в `schema_audit` после удаления базы данных  
**Статус:** ✅ ИСПРАВЛЕНО

#### 2. **Неправильное имя volume в reset.bat**
**Проблема:** Используется `planica_bi_mysql_data` вместо `docker_mysql_data`  
**Статус:** ✅ ИСПРАВЛЕНО

#### 3. **Отсутствие go.mod для Go проекта**
**Проблема:** Нет файла go.mod для управления зависимостями  
**Статус:** ⚠️ ТРЕБУЕТСЯ СОЗДАНИЕ

---

## 📝 Пошаговая инструкция для новичка

### Шаг 1: Подготовка окружения

#### 1.1. Установка Docker Desktop

1. Скачайте Docker Desktop для Windows:
   - Перейдите на https://www.docker.com/products/docker-desktop
   - Скачайте установщик
   - Запустите установщик и следуйте инструкциям
   - Перезагрузите компьютер после установки

2. Проверьте установку:
   ```powershell
   docker --version
   docker-compose --version
   ```

#### 1.2. Установка Go (если планируете использовать Go код)

1. Скачайте Go:
   - Перейдите на https://go.dev/dl/
   - Скачайте установщик для Windows
   - Запустите установщик

2. Проверьте установку:
   ```powershell
   go version
   ```

#### 1.3. Установка MySQL Connector для PowerShell (для скрипта миграций)

**Вариант 1: Через NuGet (рекомендуется)**

1. Откройте PowerShell от имени администратора
2. Установите пакет:
   ```powershell
   Install-Package MySql.Data -ProviderName NuGet -Scope CurrentUser
   ```

**Вариант 2: Через NuGet Package Manager**

1. Откройте Visual Studio
2. Tools → NuGet Package Manager → Package Manager Console
3. Выполните:
   ```powershell
   Install-Package MySql.Data
   ```

**Вариант 3: Использовать альтернативный метод подключения**

Если установка не работает, можно использовать `mysql.exe` напрямую (см. альтернативный скрипт ниже).

---

### Шаг 2: Настройка проекта

#### 2.1. Создание go.mod файла

1. Откройте командную строку в папке `database`
2. Выполните:
   ```powershell
   cd c:\planica_bi\database
   go mod init planica_bi/database
   ```

3. Добавьте зависимости:
   ```powershell
   go get github.com/go-sql-driver/mysql
   go get github.com/joho/godotenv
   ```

4. Проверьте создание файлов:
   ```powershell
   dir go.mod
   dir go.sum
   ```

#### 2.2. Создание .env файла (опционально, но рекомендуется)

1. Создайте файл `.env` в папке `database`:
   ```env
   # Development
   DB_HOST=localhost
   DB_PORT=3306
   DB_USER=planica_user
   DB_PASSWORD=root
   DB_NAME=planica_bi

   # Production (заполните при необходимости)
   PRODUCTION_DB_HOST=your-production-host
   PRODUCTION_DB_PORT=3306
   PRODUCTION_DB_USER=planica_user
   PRODUCTION_DB_PASSWORD=your-secure-password
   PRODUCTION_DB_NAME=planica_bi
   ```

2. **ВАЖНО:** Убедитесь, что `.env` в `.gitignore` (уже добавлен)

---

### Шаг 3: Запуск базы данных

#### 3.1. Первый запуск

1. Откройте командную строку в папке `database\docker`
2. Запустите:
   ```powershell
   cd c:\planica_bi\database\docker
   docker-compose up -d
   ```

   Или используйте скрипт:
   ```powershell
   cd c:\planica_bi\database
   .\docker\start.bat
   ```

3. Дождитесь запуска (30-60 секунд)

4. Проверьте статус:
   ```powershell
   docker ps
   ```

   Должны увидеть два контейнера:
   - `planica_bi_mysql`
   - `planica_bi_adminer`

#### 3.2. Проверка подключения

1. Откройте браузер
2. Перейдите на http://localhost:8080
3. Войдите в Adminer:
   - Система: `MySQL`
   - Сервер: `mysql`
   - Пользователь: `planica_user`
   - Пароль: `root`
   - База данных: `planica_bi`

4. Проверьте таблицы:
   - Должны быть созданы все таблицы
   - Должны быть загружены тестовые данные

---

### Шаг 4: Проверка миграций

#### 4.1. Проверка через Adminer

1. В Adminer выполните SQL запрос:
   ```sql
   SELECT * FROM schema_migrations ORDER BY applied_at DESC;
   ```

2. Должны увидеть записи о примененных миграциях

#### 4.2. Проверка через PowerShell скрипт (если установлен MySQL Connector)

1. Откройте PowerShell в папке `database\scripts`
2. Выполните:
   ```powershell
   cd c:\planica_bi\database\scripts
   .\migrate.ps1 -Environment Development
   ```

**Примечание:** Если MySQL Connector не установлен, используйте альтернативный метод (см. ниже).

---

### Шаг 5: Тестирование Go подключения

#### 5.1. Создание тестового файла

1. Создайте файл `test_connection.go` в папке `database`:
   ```go
   package main

   import (
       "fmt"
       "log"
       "planica_bi/database/config"
   )

   func main() {
       db, err := config.Connect("development")
       if err != nil {
           log.Fatal(err)
       }
       defer db.Close()

       repo := config.NewRepository(db)

       version, err := repo.GetDatabaseVersion()
       if err != nil {
           log.Fatal(err)
       }

       fmt.Printf("✅ Подключение успешно! Версия MySQL: %s\n", version)
   }
   ```

#### 5.2. Запуск теста

```powershell
cd c:\planica_bi\database
go run test_connection.go
```

Должно вывести: `✅ Подключение успешно! Версия MySQL: 9.5.x`

---

## 🔧 Дополнительные настройки

### Настройка для продакшена

#### 1. Изменение паролей

**В docker-compose.yml:**
```yaml
environment:
  MYSQL_ROOT_PASSWORD: ваш_надежный_пароль_root
  MYSQL_PASSWORD: ваш_надежный_пароль_user
```

**В config/database.go:**
Измените значения по умолчанию или используйте переменные окружения.

**В .env файле:**
Заполните секцию Production с реальными данными.

#### 2. Настройка бэкапов

Создайте скрипт `backup.bat`:
```bat
@echo off
echo Creating database backup...
docker exec planica_bi_mysql mysqldump -u planica_user -proot planica_bi > backup_%date:~-4,4%%date:~-7,2%%date:~-10,2%_%time:~0,2%%time:~3,2%%time:~6,2%.sql
echo Backup completed!
pause
```

#### 3. Настройка автоматических бэкапов

Используйте Windows Task Scheduler для запуска backup.bat ежедневно.

---

## 🐛 Устранение неполадок

### Проблема: Docker не запускается

**Решение:**
1. Убедитесь, что Docker Desktop запущен
2. Проверьте, что виртуализация включена в BIOS
3. Перезагрузите компьютер

### Проблема: Порт 3306 занят

**Решение:**
1. Найдите процесс, использующий порт:
   ```powershell
   netstat -ano | findstr :3306
   ```

2. Остановите процесс или измените порт в docker-compose.yml:
   ```yaml
   ports:
     - "3307:3306"  # Используйте другой порт
   ```

### Проблема: Ошибка подключения к базе

**Решение:**
1. Проверьте, что контейнер запущен:
   ```powershell
   docker ps
   ```

2. Проверьте логи:
   ```powershell
   docker logs planica_bi_mysql
   ```

3. Пересоздайте контейнеры:
   ```powershell
   .\docker\reset.bat
   ```

### Проблема: PowerShell скрипт миграций не работает

**Решение 1: Установите MySQL Connector** (см. Шаг 1.3)

**Решение 2: Используйте альтернативный метод**

Создайте файл `migrate_simple.bat`:
```bat
@echo off
echo Running migrations manually...
docker exec -i planica_bi_mysql mysql -u planica_user -proot planica_bi < ..\docker\init\02_migrations.sql
echo Migrations completed!
pause
```

### Проблема: Go модули не загружаются

**Решение:**
1. Убедитесь, что go.mod создан (см. Шаг 2.1)
2. Выполните:
   ```powershell
   go mod tidy
   go mod download
   ```

---

## ✅ Чек-лист готовности к продакшену

- [ ] Изменены все пароли по умолчанию
- [ ] Создан .env файл с реальными данными
- [ ] Настроены бэкапы базы данных
- [ ] Проверена работа всех миграций
- [ ] Протестировано Go подключение
- [ ] Проверена работа Adminer
- [ ] Настроен мониторинг базы данных
- [ ] Документированы учетные данные (в безопасном месте)

---

## 📚 Полезные команды

### Docker команды

```powershell
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Просмотр логов
docker logs planica_bi_mysql

# Вход в контейнер
docker exec -it planica_bi_mysql bash

# Выполнение SQL
docker exec -i planica_bi_mysql mysql -u planica_user -proot planica_bi -e "SHOW TABLES;"
```

### MySQL команды (внутри контейнера)

```sql
-- Показать все базы данных
SHOW DATABASES;

-- Показать все таблицы
SHOW TABLES;

-- Показать структуру таблицы
DESCRIBE projects;

-- Проверить миграции
SELECT * FROM schema_migrations ORDER BY applied_at DESC;

-- Проверить аудит
SELECT * FROM schema_audit ORDER BY changed_at DESC LIMIT 10;
```

---

## 🎯 Следующие шаги

1. ✅ Завершите настройку согласно этому руководству
2. ✅ Протестируйте все функции
3. ✅ Настройте бэкапы
4. ✅ Подготовьтесь к продакшену (измените пароли)
5. ✅ Загрузите папку `database` в репозиторий

---

**Версия:** 1.0.0  
**Дата:** 2024  
**Автор:** AI Assistant

