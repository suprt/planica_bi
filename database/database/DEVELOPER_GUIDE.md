# 🚀 Полное руководство для разработчиков - Planica BI Database

## 📋 Содержание

1. [Быстрый старт](#быстрый-старт)
2. [Структура проекта](#структура-проекта)
3. [Учетные данные](#учетные-данные)
4. [Подключение к базе данных](#подключение-к-базе-данных)
5. [Работа с миграциями](#работа-с-миграциями)
6. [Полезные команды](#полезные-команды)
7. [Решение проблем](#решение-проблем)

---

## 🚀 Быстрый старт

### Шаг 1: Установка Docker Desktop

1. Скачайте и установите Docker Desktop: https://www.docker.com/products/docker-desktop
2. Перезагрузите компьютер после установки
3. Запустите Docker Desktop
4. Проверьте установку:
   ```powershell
   docker --version
   docker-compose --version
   ```

### Шаг 2: Запуск базы данных

1. Откройте PowerShell в папке проекта:
   ```powershell
   cd c:\planica_bi\database
   ```

2. Запустите базу данных:
   ```powershell
   .\docker\start.bat
   ```
   
   Или вручную:
   ```powershell
   cd docker
   docker-compose up -d
   ```

3. Подождите 30-60 секунд для инициализации

4. Проверьте статус:
   ```powershell
   docker ps
   ```
   
   Должны увидеть два контейнера:
   - `planica_bi_mysql`
   - `planica_bi_adminer`

### Шаг 3: Проверка работы

1. Откройте браузер: http://localhost:8080
2. Войдите в Adminer:
   - **Система:** `MySQL`
   - **Сервер:** `mysql`
   - **Пользователь:** `planica_user`
   - **Пароль:** `root`
   - **База данных:** `planica_bi`
3. Нажмите "Войти"

---

## 📁 Структура проекта

```
database/
├── .env                          # Переменные окружения (НЕ коммитить!)
├── .gitignore                    # Игнорируемые файлы
├── go.mod                        # Go зависимости
├── go.sum                        # Go checksums
├── README.md                     # Основная документация
├── SETUP_GUIDE.md               # Детальное руководство для новичков
├── DEVELOPER_GUIDE.md           # Это руководство
├── PRODUCTION_CHECKLIST.md      # Чек-лист для продакшена
├── CHANGES_REPORT.md            # Отчет об изменениях
├── FINAL_REPORT.md              # Финальный отчет
├── test_connection.go            # Тестовый файл подключения
│
├── config/                       # Go конфигурация
│   └── database.go              # Подключение к БД
│
├── docker/                       # Docker конфигурация
│   ├── docker-compose.yml       # Конфигурация контейнеров
│   ├── my.cnf                   # Настройки MySQL
│   ├── start.bat                # Запуск контейнеров
│   ├── stop.bat                 # Остановка контейнеров
│   ├── reset.bat                # Сброс базы данных
│   └── init/                    # SQL скрипты инициализации
│       ├── 01_schema.sql        # Создание БД и пользователя
│       ├── 02_migrations.sql    # Создание всех таблиц
│       └── 03_seeds.sql         # Тестовые данные
│
├── migrations/                   # Система миграций
│   ├── 001_init/                # Инициализация БД
│   │   ├── 001_create_database.up.sql
│   │   └── 001_create_database.down.sql
│   ├── 002_core/                # Основные таблицы (проекты)
│   ├── 003_analytics/           # Яндекс.Метрика и Директ
│   ├── 004_metrics/             # Таблицы метрик
│   ├── 005_seo/                 # SEO данные
│   └── 006_users/               # Пользователи и роли
│
├── seeds/                        # Тестовые данные
│   ├── 001_sample_projects.sql
│   ├── 002_sample_analytics.sql
│   ├── 003_sample_seo.sql
│   └── 004_sample_users.sql
│
└── scripts/                      # Скрипты управления
    ├── migrate.ps1              # PowerShell скрипт миграций
    ├── migrate_complete.bat     # Запуск миграций
    └── rollback.bat             # Откат миграций
```

---

## 🔐 Учетные данные

### ⚠️ ВАЖНО: Эти пароли только для разработки!

### Для подключения к базе данных:

- **Хост:** `localhost`
- **Порт:** `3306`
- **Пользователь:** `planica_user`
- **Пароль:** `root`
- **База данных:** `planica_bi`

### Для root пользователя MySQL:

- **Пользователь:** `root`
- **Пароль:** `root`

### Для Adminer (веб-интерфейс):

- **URL:** http://localhost:8080
- **Система:** `MySQL`
- **Сервер:** `mysql`
- **Пользователь:** `planica_user`
- **Пароль:** `root`
- **База данных:** `planica_bi`

### Переменные окружения (.env файл):

```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=planica_user
DB_PASSWORD=root
DB_NAME=planica_bi
```

---

## 🔌 Подключение к базе данных

### Через Go (config/database.go)

```go
package main

import (
    "log"
    "planica_bi/database/config"
)

func main() {
    // Подключение к базе данных
    db, err := config.Connect("development")
    if err != nil {
        log.Fatal(err)
    }
    defer db.Close()
    
    // Создание репозитория
    repo := config.NewRepository(db)
    
    // Проверка подключения
    if err := repo.HealthCheck(); err != nil {
        log.Fatal(err)
    }
    
    // Получение версии MySQL
    version, err := repo.GetDatabaseVersion()
    if err != nil {
        log.Fatal(err)
    }
    log.Printf("MySQL version: %s", version)
}
```

### Через командную строку

```powershell
# Вход в контейнер MySQL
docker exec -it planica_bi_mysql mysql -u planica_user -proot planica_bi

# Выполнение SQL запроса
docker exec -i planica_bi_mysql mysql -u planica_user -proot planica_bi -e "SHOW TABLES;"
```

### Через Adminer (веб-интерфейс)

1. Откройте http://localhost:8080
2. Введите учетные данные (см. раздел "Учетные данные")
3. Нажмите "Войти"

---

## 📊 Структура базы данных

### Основные таблицы:

- **projects** - Проекты
- **users** - Пользователи системы
- **user_project_roles** - Роли пользователей в проектах

### Аналитика:

- **yandex_counters** - Счетчики Яндекс.Метрики
- **direct_accounts** - Аккаунты Яндекс.Директа
- **direct_campaigns** - Кампании Директа
- **goals** - Цели Яндекс.Метрики

### Метрики:

- **metrics_monthly** - Основные метрики помесячно
- **metrics_age_monthly** - Метрики по возрастным группам
- **direct_campaign_monthly** - Метрики Директа по кампаниям
- **direct_totals_monthly** - Итоговые метрики Директа

### SEO:

- **seo_queries_monthly** - SEO запросы помесячно
- **seo_summary_monthly** - Сводные SEO метрики

### Системные:

- **schema_migrations** - История миграций
- **schema_audit** - Аудит изменений схемы

---

## 🔄 Работа с миграциями

### Автоматические миграции (Docker)

При первом запуске Docker контейнера все миграции применяются автоматически через `docker/init/02_migrations.sql`.

### Ручные миграции (PowerShell)

```powershell
cd scripts
.\migrate.ps1 -Environment Development
```

**Примечание:** Требуется установка MySQL Connector для PowerShell (см. SETUP_GUIDE.md).

### Проверка статуса миграций

```sql
SELECT * FROM schema_migrations ORDER BY applied_at DESC;
```

---

## 🛠️ Полезные команды

### Docker команды

```powershell
# Запуск
.\docker\start.bat
# или
cd docker
docker-compose up -d

# Остановка
.\docker\stop.bat
# или
cd docker
docker-compose down

# Сброс базы данных (удалит все данные!)
.\docker\reset.bat

# Просмотр логов
docker logs planica_bi_mysql

# Вход в контейнер
docker exec -it planica_bi_mysql bash

# Выполнение SQL
docker exec -i planica_bi_mysql mysql -u planica_user -proot planica_bi -e "SHOW TABLES;"
```

### MySQL команды (внутри контейнера)

```sql
-- Показать все базы данных
SHOW DATABASES;

-- Показать все таблицы
SHOW TABLES;

-- Показать структуру таблицы
DESCRIBE projects;

-- Проверить миграции
SELECT * FROM schema_migrations ORDER BY applied_at DESC;

-- Проверить аудит
SELECT * FROM schema_audit ORDER BY changed_at DESC LIMIT 10;

-- Показать тестовые проекты
SELECT id, name, slug, status FROM projects;
```

### Go команды

```powershell
# Установка зависимостей
go mod download

# Обновление зависимостей
go mod tidy

# Запуск тестового подключения
go run test_connection.go

# Сборка проекта
go build
```

---

## 🐛 Решение проблем

### Проблема: Docker не запускается

**Решение:**
1. Убедитесь, что Docker Desktop запущен
2. Проверьте, что виртуализация включена в BIOS
3. Перезагрузите компьютер

### Проблема: Порт 3306 занят

**Решение:**
1. Найдите процесс, использующий порт:
   ```powershell
   netstat -ano | findstr :3306
   ```
2. Остановите процесс или измените порт в `docker-compose.yml`:
   ```yaml
   ports:
     - "3307:3306"  # Используйте другой порт
   ```

### Проблема: Ошибка подключения к базе

**Решение:**
1. Проверьте, что контейнер запущен:
   ```powershell
   docker ps
   ```
2. Проверьте логи:
   ```powershell
   docker logs planica_bi_mysql
   ```
3. Пересоздайте контейнеры:
   ```powershell
   .\docker\reset.bat
   ```

### Проблема: Go модули не загружаются

**Решение:**
1. Убедитесь, что Go установлен:
   ```powershell
   go version
   ```
2. Выполните:
   ```powershell
   go mod tidy
   go mod download
   ```

### Проблема: Ошибка "missing go.sum entry"

**Решение:**
```powershell
go mod tidy
go get planica_bi/database/config
```

---

## 📝 Тестовые данные

База данных автоматически загружается тестовыми данными при первом запуске:

- **3 тестовых проекта:**
  - Интернет-магазин электроники
  - Строительная компания "СтройГарант"
  - Туристическое агентство "Вокруг света"

- **4 тестовых пользователя:**
  - Администратор системы (admin@planica.ru)
  - Менеджер проектов (manager@planica.ru)
  - Клиент Иванов (client@electronics-store.ru)
  - Клиент Петров (client@construction-company.ru)

- **Тестовые данные аналитики:**
  - Счетчики Яндекс.Метрики
  - Аккаунты и кампании Директа
  - Метрики за ноябрь 2024
  - SEO данные

---

## 🔒 Безопасность для продакшена

### ⚠️ ВАЖНО: Перед развертыванием в продакшен:

1. **Измените все пароли:**
   - В `docker-compose.yml`
   - В `config/database.go`
   - В `.env` файле

2. **Используйте переменные окружения:**
   - Не храните пароли в коде
   - Используйте `.env` файлы (не коммитьте их!)

3. **Настройте бэкапы:**
   - См. `SETUP_GUIDE.md`, раздел "Настройка бэкапов"

4. **Проверьте чек-лист:**
   - Откройте `PRODUCTION_CHECKLIST.md`
   - Выполните все пункты

---

## 📚 Дополнительная документация

- **README.md** - Основная документация проекта
- **SETUP_GUIDE.md** - Детальное руководство для новичков
- **PRODUCTION_CHECKLIST.md** - Чек-лист готовности к продакшену
- **CHANGES_REPORT.md** - Отчет об изменениях
- **FINAL_REPORT.md** - Финальный отчет проверки

---

## 🎯 Быстрая справка

### Запуск базы данных:
```powershell
.\docker\start.bat
```

### Остановка базы данных:
```powershell
.\docker\stop.bat
```

### Сброс базы данных:
```powershell
.\docker\reset.bat
```

### Веб-интерфейс:
http://localhost:8080

### Учетные данные:
- Пользователь: `planica_user`
- Пароль: `root`
- База: `planica_bi`

---

## ✅ Чек-лист перед началом работы

- [ ] Docker Desktop установлен и запущен
- [ ] База данных запущена (`docker ps` показывает контейнеры)
- [ ] Adminer доступен (http://localhost:8080)
- [ ] Подключение к базе работает
- [ ] Тестовые данные загружены
- [ ] Go установлен (если планируете использовать Go код)
- [ ] Go зависимости установлены (`go mod download`)

---

**Версия:** 1.0.0  
**Дата:** 2024  
**Для:** Разработчики Planica BI  
**Статус:** ✅ Готово к использованию

---

## 💡 Полезные ссылки

- [Docker Desktop](https://www.docker.com/products/docker-desktop)
- [Go Downloads](https://go.dev/dl/)
- [MySQL Documentation](https://dev.mysql.com/doc/)
- [Adminer Documentation](https://www.adminer.org/)

---

**Удачи на хакатоне! 🚀**

