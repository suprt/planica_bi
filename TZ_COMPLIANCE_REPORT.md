# Отчет о соответствии проекта ТЗ

## ✅ Что реализовано правильно

### 1. Структура данных (пункт 1)
- ✅ `projects` - все поля соответствуют ТЗ
- ✅ `yandex_counters` - соответствует
- ✅ `direct_accounts` - соответствует
- ✅ `direct_campaigns` - соответствует
- ✅ `goals` - соответствует
- ✅ `metrics_monthly` - соответствует
- ✅ `metrics_age_monthly` - соответствует (возрастные группы стандартные из Метрики)
- ✅ `direct_campaign_monthly` - соответствует (есть `project_id` и `direct_campaign_id`)
- ✅ `direct_totals_monthly` - соответствует
- ✅ `seo_queries_monthly` - соответствует

### 2. Интеграции (пункт 2)
- ✅ Яндекс OAuth - реализовано
- ✅ Яндекс.Метрика API - реализовано (visits, users, bounceRate, avgVisitDurationSeconds)
- ✅ Разбивка по возрасту - реализовано
- ✅ Яндекс.Директ API - реализовано (Reports API с Client-Login)
- ✅ Метрики Директа: Impressions, Clicks, Cost, CTR, AvgCpc

### 3. API (пункт 6)
- ✅ `POST /api/projects` - создание проекта (admin)
- ✅ `POST /api/projects/{id}/counters` - привязка счётчиков (manager/admin)
- ✅ `POST /api/projects/{id}/direct-accounts` - привязка логина Директа (manager/admin)
- ✅ `POST /api/projects/{id}/goals` - регистрация целей (manager/admin)
- ✅ `POST /api/sync/{id}` - форс-синхронизация (admin)
- ✅ `GET /api/report/{id}` - JSON отчёт для 3 месяцев (M, M-1, M-2)
- ✅ Формат JSON соответствует контракту из ТЗ

### 4. Безопасность (пункт 7)
- ✅ Роли: admin, manager, client - реализованы
- ✅ Авторизация email+password - реализована (JWT)
- ✅ Доступ по проектам через `user_project_roles` - реализован
- ✅ Admin видит все проекты - реализовано
- ✅ Manager/client видят только свои проекты - реализовано

### 5. Расчёты (пункт 3)
- ✅ Три месяца: M (текущий), M-1, M-2 - реализовано
- ✅ Динамика %: `(M − M-1)/M-1 * 100%` - реализовано
- ✅ При M-1 = 0 → отображается +0 - реализовано
- ✅ Возрастные группы: 18-24, 25-34, 35-44, 45-54, 55+, unknown - реализовано

## ❌ Критические несоответствия

### 1. Актуализация: Filament для админа
**ТЗ (актуализация):** "админ управляет проектами через Filament"
**Текущее состояние:** Нет Filament, только REST API
**Критичность:** Высокая (требование из актуализации)
**Решение:** 
- Вариант 1: Интегрировать Filament для Go (если есть)
- Вариант 2: Использовать готовый админ-интерфейс (например, AdminJS)
- Вариант 3: Создать простой веб-интерфейс для админа

### 2. Актуализация: Публичная ссылка для клиента
**ТЗ (актуализация):** "клиент видит публичную ссылку отчёта (только просмотр)"
**Текущее состояние:** Все эндпоинты требуют авторизацию
**Критичность:** Высокая (требование из актуализации)
**Решение:** 
- Добавить публичный эндпоинт `/api/public/report/{slug}` или `/api/public/report/{token}`
- Использовать slug проекта или токен доступа
- Убрать требование авторизации для этого эндпоинта

### 3. Синхронизация по крону (пункт 5, актуализация)
**ТЗ:** 
- Ежедневно ночью: обновление данных M (текущего месяца)
- 1-го числа каждого месяца в 07:00 MSK: фиксация итогов прошедшего месяца
**Текущее состояние:** Только заглушка в `backend/internal/cron/scheduler.go`
**Критичность:** Высокая (критерий приёмки пункт 8.5)
**Решение:**
- Реализовать cron scheduler (использовать библиотеку `github.com/robfig/cron/v3`)
- Настроить задачи:
  - Ежедневно в 02:00 MSK: `SyncAllProjects` (обновление текущего месяца)
  - 1-го числа в 07:00 MSK: `FinalizeMonth` (фиксация прошедшего месяца)

### 4. Очереди для API-вызовов (пункт 5)
**ТЗ:** "Очереди: Laravel Queue (database/redis) для API‑вызовов и парсинга отчётов"
**Текущее состояние:** ❌ Полностью отсутствуют
- Все API-вызовы к Яндекс.Метрике/Директу выполняются синхронно
- При синхронизации проекта HTTP-запрос блокируется до завершения всех API-вызовов
- Нет обработки ошибок с retry механизмом
- Нет приоритизации задач
**Критичность:** Высокая (требование из ТЗ, влияет на производительность)
**Решение:**
- Интегрировать систему очередей (например, `github.com/hibiken/asynq` - простая и эффективная)
- Вынести синхронизацию в фоновые задачи:
  - `SyncMetricaData` → очередь
  - `SyncDirectData` → очередь
  - `ParseReport` → очередь
- Настроить воркеры для обработки очередей
- Добавить retry механизм для неудачных API-вызовов

## ⚠️ Потенциальные несоответствия

### 1. Структура `direct_campaign_monthly`
**ТЗ:** `direct_campaign_monthly` должен иметь `project_id` (FK → projects)
**Текущее состояние:** Есть и `project_id`, и `direct_campaign_id` (FK → direct_campaigns)
**Статус:** ✅ Соответствует (оба поля есть, что правильно для денормализации)

### 2. Поле `ctr_pct` vs `ctr`
**ТЗ:** В таблице указано `ctr_pct` (DECIMAL(6,2))
**Текущее состояние:** В моделях используется `CTRPct` (соответствует)
**Статус:** ✅ Соответствует

### 3. Экспорт PDF
**ТЗ (пункт 8.4):** "Экспорт отчёта в PDF"
**Актуализация:** "Экспорт PDF: не требуется в рамках MVP"
**Статус:** ✅ Соответствует актуализации (не требуется)

### 4. Frontend
**ТЗ:** "Frontend (MVP): HTML + нативный JS"
**Текущее состояние:** Нет frontend (только API)
**Критичность:** Средняя (для MVP нужен хотя бы базовый интерфейс)
**Решение:** Создать простой HTML+JS интерфейс для просмотра отчётов

## 📋 Рекомендации по приоритетам

### Критично (для соответствия ТЗ):
1. ✅ Реализовать cron scheduler для синхронизации
2. ✅ Добавить публичную ссылку для клиентов
3. ✅ Реализовать очереди для API-вызовов (asynq + Redis)
4. ⚠️ Filament для админа (можно заменить на простой админ-интерфейс)

### Важно (для производительности):
5. Настроить логирование в файлы (`storage/logs`)
6. Реализовать кэш Redis для справочников

### Желательно (для полноты MVP):
7. Создать базовый HTML+JS frontend для просмотра отчётов
8. Добавить уведомления в Telegram/Sentry (опционально)

### 5. Логирование (пункт 5)
**ТЗ:** "Логирование: storage/logs + уведомления об ошибках в Telegram/Sentry (опция)"
**Текущее состояние:** ⚠️ Частично реализовано
- ✅ Есть zap logger
- ❌ Логи пишутся только в stdout (не в `storage/logs`)
- ❌ Нет уведомлений в Telegram
- ❌ Нет интеграции с Sentry
**Критичность:** Средняя (для MVP достаточно базового логирования)
**Решение:**
- Настроить zap для записи в файл `storage/logs/app.log`
- Добавить ротацию логов
- Опционально: добавить интеграцию с Telegram Bot API для критических ошибок
- Опционально: добавить интеграцию с Sentry

### 6. Кэш Redis (пункт 5)
**ТЗ:** "Кэш: Redis для кэширования неизменных справочников (кампании и пр.)"
**Текущее состояние:** ❌ Полностью отсутствует
- Redis настроен в `.env`, но не используется
- Нет подключения к Redis
- Нет кэширования справочников (кампании, цели, счётчики)
- Каждый запрос идёт в БД
**Критичность:** Средняя (для MVP можно без кэша, но для производительности желательно)
**Решение:**
- Подключить Redis клиент (`github.com/redis/go-redis/v9`)
- Создать cache service для работы с Redis
- Кэшировать:
  - Список кампаний проекта (TTL: 1 час)
  - Список целей счётчика (TTL: 1 час)
  - Список счётчиков проекта (TTL: 1 час)
- Инвалидировать кэш при обновлении данных

## 📊 Итоговая оценка соответствия

- **Структура данных:** 100% ✅
- **API:** 100% ✅
- **Интеграции:** 100% ✅
- **Безопасность:** 90% ⚠️ (нет публичной ссылки)
- **Синхронизация:** 20% ❌ (только заглушка)
- **Очереди:** 0% ❌ (полностью отсутствуют)
- **Логирование:** 50% ⚠️ (есть logger, но нет файлов и уведомлений)
- **Кэш Redis:** 0% ❌ (настроен, но не используется)
- **Админ-панель:** 0% ❌ (нет Filament)
- **Frontend:** 0% ⚠️ (нет интерфейса)

**Общее соответствие:** ~60%

