# База данных Planica BI

Профессиональная система управления базой данных для проекта Planica BI.

## 🚀 Быстрый старт

### Требования
- MySQL Server 9.5+ (или Docker)
- Go 1.21+ (для работы с репозиториями)
- Docker и Docker Compose (для локальной разработки)

### Установка с Docker (рекомендуется)

1. **Настройте переменные окружения:**
   
   Убедитесь, что в корне `backend/` есть файл `.env` с настройками базы данных:
   ```env
   DB_HOST=mysql
   DB_PORT=3306
   DB_DATABASE=planica_bi
   DB_USERNAME=planica_user
   DB_PASSWORD=root
   APP_PORT=8080
   ```
   
   **Важно:** В Docker Compose используйте `DB_HOST=mysql` (имя сервиса), а не `127.0.0.1`.
   
   Docker Compose автоматически использует эти переменные из `.env` файла.

2. **Запустите все сервисы:**
   ```bash
   docker-compose up -d
   ```
   
   Это запустит:
   - MySQL базу данных
   - Backend API
   - Adminer (веб-интерфейс для БД)

3. **Проверьте статус:**
   - MySQL доступен на `localhost:3306`
   - Backend API доступен на `http://localhost:8080`
   - Adminer доступен на `http://localhost:8081`
   - Учетные данные для БД берутся из `.env` файла (по умолчанию):
     - Пользователь: `planica_user` (из `DB_USERNAME`)
     - Пароль: `root` (из `DB_PASSWORD`)
     - База данных: `planica_bi` (из `DB_DATABASE`)

4. **База данных автоматически инициализируется** при первом запуске через файлы в `backend/database/init/`:
   - `01_schema.sql` - создание базы и пользователя
   - `02_migrations.sql` - создание всех таблиц
   - `03_seeds.sql` - тестовые данные

### Остановка сервисов

```bash
docker-compose down
```

### Сброс базы данных

```bash
docker-compose down -v
docker-compose up -d
```

⚠️ **Внимание:** Это удалит все данные!

### Запуск только базы данных

Если нужно запустить только MySQL без backend:

```bash
docker-compose up -d mysql adminer
```

## 📁 Структура проекта

```
planica_bi/
├── docker-compose.yml   # Основной Docker Compose файл для всех сервисов
├── backend/
│   ├── .env             # Переменные окружения (включая настройки БД)
│   ├── Dockerfile       # Dockerfile для backend приложения
│   ├── database/        # База данных
│   │   ├── my.cnf       # Настройки MySQL
│   │   ├── init/        # SQL скрипты для инициализации
│   │   │   ├── 01_schema.sql
│   │   │   ├── 02_migrations.sql
│   │   │   └── 03_seeds.sql
│   │   ├── migrations/  # Система миграций
│   │   │   ├── 001_init/    # Инициализация БД
│   │   │   ├── 002_core/    # Основные таблицы (проекты)
│   │   │   ├── 003_analytics/ # Яндекс.Метрика и Директ
│   │   │   ├── 004_metrics/ # Таблицы метрик
│   │   │   ├── 005_seo/     # SEO данные
│   │   │   └── 006_users/   # Пользователи и роли
│   │   └── seeds/       # Тестовые данные
│   │       ├── 001_sample_projects.sql
│   │       ├── 002_sample_analytics.sql
│   │       └── 003_sample_seo.sql
│   └── internal/
│       └── database/    # Go конфигурация подключения к БД
│           └── database.go
```

## 🗄️ Структура базы данных

### Основные таблицы

- **projects** - Проекты
- **users** - Пользователи системы
- **user_project_roles** - Роли пользователей в проектах

### Аналитика

- **yandex_counters** - Счетчики Яндекс.Метрики
- **direct_accounts** - Аккаунты Яндекс.Директа
- **direct_campaigns** - Кампании Директа
- **goals** - Цели Яндекс.Метрики

### Метрики

- **metrics_monthly** - Основные метрики помесячно
- **metrics_age_monthly** - Метрики по возрастным группам
- **direct_campaign_monthly** - Метрики Директа по кампаниям
- **direct_totals_monthly** - Итоговые метрики Директа

### SEO

- **seo_queries_monthly** - SEO запросы помесячно

### Системные

- **schema_migrations** - История миграций
- **schema_audit** - Аудит изменений схемы

## 🔧 Использование Go конфигурации

Конфигурация базы данных находится в `backend/internal/database/database.go` и использует переменные окружения из `.env` файла:

```go
package main

import (
    "gitlab.ugatu.su/gantseff/planica_bi/backend/internal/config"
    "gitlab.ugatu.su/gantseff/planica_bi/backend/internal/database"
)

func main() {
    cfg := config.Load()
    
    // Подключение к базе данных
    db, err := database.Connect(cfg)
    if err != nil {
        log.Fatal(err)
    }
    defer database.Close()
    
    // Запуск миграций
    if err := database.AutoMigrate(); err != nil {
        log.Fatal(err)
    }
}
```

## 🔄 Миграции

### Автоматические миграции (Docker)

При первом запуске Docker контейнера все миграции применяются автоматически через `docker/init/02_migrations.sql`.

### Автоматические миграции (GORM)

Приложение автоматически применяет миграции через GORM AutoMigrate при запуске (см. `backend/internal/database/database.go`).

## 📊 Тестовые данные

Тестовые данные автоматически загружаются при первом запуске через `docker/init/03_seeds.sql`.

Также можно загрузить их вручную из папки `seeds/`:
- `001_sample_projects.sql` - Тестовые проекты
- `002_sample_analytics.sql` - Тестовые данные аналитики
- `003_sample_seo.sql` - Тестовые SEO данные
- `004_sample_users.sql` - Тестовые пользователи

## 🔐 Безопасность

⚠️ **Важно для продакшена:**

1. **Пароли:** Измените пароли по умолчанию в `.env` файле
2. **Токены:** Токены доступа к API хранятся в зашифрованном виде в полях `access_token_encrypted`
3. **Переменные окружения:** Используйте `.env` файлы для хранения секретов (не коммитьте их в Git!)

## 🌍 Переменные окружения

Создайте файл `.env` в корне `backend/` (он уже используется для всех настроек приложения):

```env
# Database configuration
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=planica_bi
DB_USERNAME=planica_user
DB_PASSWORD=root

# Other application settings...
APP_ENV=development
JWT_SECRET=your-secret-key-change-in-production
# ... и т.д.
```

Docker Compose автоматически читает эти переменные из `.env` файла при запуске контейнеров.

## 📝 Примечания

- Все таблицы используют кодировку `utf8mb4` для поддержки эмодзи и специальных символов
- Все таблицы используют движок `InnoDB` для поддержки транзакций и внешних ключей
- Таблицы с большим объемом данных используют `ROW_FORMAT=COMPRESSED` для экономии места
- Все даты хранятся в формате `TIMESTAMP` с автоматическим обновлением

## 🐛 Устранение неполадок

### База данных не запускается

1. Проверьте, что порт 3306 свободен:
   ```bash
   netstat -an | findstr 3306
   ```

2. Проверьте логи Docker:
   ```bash
   docker logs planica_bi_mysql
   ```

3. Пересоздайте контейнеры:
   ```bash
   docker-compose down -v
   docker-compose up -d
   ```

### Ошибки подключения

1. Убедитесь, что контейнер запущен:
   ```bash
   docker ps
   ```

2. Проверьте учетные данные в `.env` файле в корне `backend/`

3. Проверьте, что база данных создана:
   ```sql
   SHOW DATABASES;
   ```

## 📚 Дополнительная информация

- [MySQL 9.5 Documentation](https://dev.mysql.com/doc/refman/9.5/en/)
- [Docker Compose Documentation](https://docs.docker.com/compose/)
- [Go database/sql Tutorial](https://go.dev/doc/database/)

## 👥 Поддержка

При возникновении проблем:
1. Проверьте логи Docker контейнеров
2. Проверьте статус миграций в таблице `schema_migrations`
3. Проверьте аудит изменений в таблице `schema_audit`

---

**Версия:** 1.0.0  
**Последнее обновление:** 2024
